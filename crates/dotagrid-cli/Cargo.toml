[package]
name = "dotagrid-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.104"
clap = "4.6.4"
dota-hero-grid = { version = "0.1.0", path = "../dota-hero-grid" }
toml = { version = "1.1.4", features = ["parse"] }
